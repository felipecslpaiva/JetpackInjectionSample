package gg.paiva.jetpackexploration.session

import android.util.Log
import androidx.work.WorkManager

class SessionController(workManager : WorkManager) {
    init {
        Log.d("","Controller init" )
    }
}